/* eslint-disable no-empty-pattern */
import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const datveSlide = createSlice({
  name: 'datveSlide',
  initialState,
  reducers: {}
});

export const {} = datveSlide.actions

export default datveSlide.reducer